package scenes;

import java.awt.Graphics;

import main.Game;

public class Settings extends GameScene implements SceneMethods {

    public Settings(Game game) {
        super(game);
    }

    @Override
    public void render(Graphics g) {
        // Rendering logic for the settings scene
    }

    @Override
    public void mouseClicked(int x, int y) {
        // Handle mouse click events in the settings scene
    }

    @Override
    public void mouseMoved(int x, int y) {
        // Handle mouse movement events in the settings scene
    }

    @Override
    public void mousePressed(int x, int y) {
        // Handle mouse press events in the settings scene
    }

    @Override
    public void mouseReleased(int x, int y) {
        // Handle mouse release events in the settings scene
    }
    
}
